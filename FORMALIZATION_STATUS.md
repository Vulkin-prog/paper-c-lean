# Formalization status, section by section

Legend:

- **Proved**: compiled Lean theorem, with no `sorry` and no added axiom.
- **Defined**: objects formalized, but the paper's result has not yet been proved.
- **To be formalized**: argument internal to the manuscript that remains to be translated.
- **External bridge**: cited theorem for which a Lean proof is missing.
- **Internal bridge**: interface arising from an argument proved in the manuscript itself.

Version 0.48.1 changes only the bilingual manuscript PDFs and their public
release metadata. The Lean sources, Comparator interfaces, declaration and
audit-target counts, bridge registry, and toolchain remain byte-identical to
v0.48.0. The published v0.48.0 hardened evidence therefore remains evidence
for the unchanged Comparator fileset, but it does not bind the corrected
v0.48.1 PDF byte streams.

In the audit registry, `kind: external | internal` records the provenance of
the statement. The separate field `status: open | discharged` records whether
the interface is still required: only `open` constitutes current formalization
debt. In v0.48.0, with Lean/mathlib unchanged at `v4.32.2`, all five
`internal` interfaces remain `discharged`, while all seven `open` interfaces
remain `external`. The 13-entry registry is unchanged: eight `external`, five
`internal`, seven `open` (all `external`), and six `discharged`. Exact
declaration and audit-target counts are generated in `audit_manifest.json`:
4,065 theorems and five lemmas, hence 4,070 public declarations and 4,072
audit targets, split into 3,912 declarations without a registered bridge
premise and 158 conditional declarations. They are reproduced in
`REPRODUCIBILITY.md`.

## External semantic audit boundary (v0.48.0)

The 382-file v0.47.0 core is byte-identical in v0.48.0. This release adds a
human-readable project-independent boundary and solution wrappers; it does
not add a mathematical lemma to the Paper C core or change a canonical
signature. `Challenge.lean` imports only Mathlib modules, while
`ChallengeTransfer.lean` imports exactly `Challenge` and no project module.

| Target | Statement and proof route | Present status |
|---|---|---|
| `Challenge.lean` / `Solution.lean` | `paper_c_theorem_one_one_finite_cylinder`, the quantitative finite-cylinder form of Theorem 1.1 (printed/PDF p. 3), closed by `PaperC.CorollaryThirteenTen.theorem_one_one_uniformBigO_canonical` | `Solution.lean` directly reproduces the complete declarative interface in the fresh `PaperCAudit` namespace. The Challenge file has one intentional `by sorry`; Solution has none and does not import Challenge. The byte-identical pair passed the hardened non-root Comparator procedure at Paper C commit `27c91f8bdd5c...`, with Lean-kernel acceptance and exit 0. The final v0.48.0 metadata layer leaves the Comparator fileset unchanged. |
| `ChallengeTransfer.lean` / `SolutionTransfer.lean` | `paper_c_theorem_one_one_infinite_finite_law_identity`, the exact equality between the infinite-product law and the finite-cylinder law | `SolutionTransfer.lean` directly reproduces the transfer interface without importing a Challenge module. The independent transfer pair passed the same hardened non-root procedure in its own clean checkout. The identity itself is unconditional. |
| `formalization.yaml` v0.3 | Generated with `audit_manifest.json` from schema-2 `audit_config.json`; maps manuscript items, pages, declarations, files, consumed bridges, conditionality, and Comparator coverage | Generated metadata is a deterministic audit artifact. Its review status is `agent-reviewed`; completeness of the editorial item list still requires human review. |
| `literature_certificates/*.md` | Four source-to-proposition counter-audit reports, in their own SHA-256-bound fileset | AGG, Evertse--Silverman, and Nicolas--Robin are `agent_checked_supports`; Halter--Koch is `agent_checked_with_open_gap`. These are documentary agent statuses, not Lean discharges or human peer review. |

The manuscript states Theorem 1.1 unconditionally. The finite-cylinder
Comparator statement instead takes exactly the four ordinary literature
premises consumed by the canonical endpoint:

| Premise | Registered bridge | Formalization relation |
|---|---|---|
| Arratia--Goldstein--Gordon | `AGG89-T1-finite-dependency-b3-zero` | Finite dependency-neighborhood theorem with `b₃ = 0`, used for the Chen--Stein TV bound |
| Evertse--Silverman | `ES86-T1b-Q-split-n2` | Split quadratic, exponent-two specialization supplying the abscissa count of Lemma 9.1 |
| Halter--Koch | `HK13-QO-conductor-fibres` | Intended conductor and unit-coset comparison for Lemma 9.2; the rc2 audit has **not established** the implication from the cited pages to the full Lean record |
| Nicolas--Robin | `NR83-T1-divisor-log-bound` | Direct logarithmic divisor inequality used in the same Pell reconstruction |

They are explicit propositions and theorem arguments, not Lean axioms.  The
main Challenge presents the observable on the finite uniform cylinder of
primes up to `2*N+L`; the transfer target records the exact relation to the
paper's infinite product.  `UniformBigOOn` expands all uniformity quantifiers,
the critical window is `|L-log N/log 2| ≤ C`, and total variation uses the
half-`ℓ¹` normalization.  The interface accepts `C ≥ 0`, a harmless
strengthening of the paper's `C > 0`.  The global longest-run result is not in
this Comparator surface.  `Solution.lean` makes explicit the two
non-definitional record translations needed to connect the readable interface
to the frozen core: the Arratia--Goldstein--Gordon finite-probability record
and the Halter--Koch quadratic-order conductor record. This last translation
starts from the explicit Challenge premise; it does not establish that the
cited Halter--Koch results imply that premise.

Both JSON configurations explicitly permit only `propext`, `Quot.sound`, and
`Classical.choice`, and both have `enable_nanoda: false`. Nanoda was not run
and no dual-kernel result is claimed. The two source-file Challenge
placeholders are counted separately from the proof-side `sorry_count = 0`.
The hardened main and transfer transcripts have SHA-256 values
`fed5cf1fd82037c98c1b3f4c713189958ac92b5ceb58982e0e1093f31ce7599e`
and
`13703cb6794c4102b6d172e6632fddb1df75d6d47dfa32e46f63030adea33076`.
The raw transcripts remain local and unpublished because they contain
host-identifying metadata; their SHA-256 values above remain bound by the
public redaction manifest and result records. The published privacy-minimized
`paper-c-hardened-public-27c91f8.tar.zst` archive is 49,765 bytes; its SHA-256
is
`7279ed99e6b1a98b6458fe1f8d95dd1f68af445646ee6a23d0044c7bee94ce50`
and its byte-identical summary SHA-256 is
`b1d956eeee5451a64dbbdab495fe2f56f22b7ee06e2d65624394b3fee5ca41fb`.
The redaction manifest SHA-256 is
`01c3619d80786999a323c8b2073c4e674bf97af46f179b7e1434af53f3b6f29b`.
This public evidence records and binds the Comparator result at the recorded
input commit, but does not permit line-by-line public inspection of the full
host trace. It does not establish the four source-to-proposition implications
or paper-to-Challenge fidelity.

## Sections 1–3

| Manuscript | Content | Status |
|---|---|---|
| Th. 1.1 | Critical Poisson law and TV rate | **Conditionally proved on both the finite cylinder and the infinite source law, modulo the literature only.** In the exact critical window, Lean assembles the uniform rate \(d_{\rm TV}(\mathcal L(Z_{N,L}),\operatorname{Pois}(N2^{-L}))\ll_C(\log\log N)^{-2}\). The random variable `infiniteDyadicStartCount` is defined on the infinite Rademacher product space, its law is proved exactly equal to `fullDyadicStartLaw`, and `theorem_one_one_infinite_model` transports the same endpoint without any new premise. The only bridges are AGG, Evertse–Silverman, Halter–Koch's quadratic-order comparison, and the Nicolas–Robin divisor bound, all `external/open`. The finite-cylinder and transfer boundaries expose these two steps separately and both passed the hardened non-root Comparator procedure at commit `27c91f8bdd5c...` with Lean-kernel acceptance; the final metadata layer leaves their fileset unchanged. Nanoda was disabled. The source reports support the AGG, Evertse–Silverman, and Nicolas–Robin mappings at agent-review level; Halter–Koch remains not established, and no independent human review is claimed. |
| Th. 1.2(i) | Deterministic masks | **Conditionally proved, modulo the literature only.** Proposition 14.1 is proved in the exact window \(\lvert L-\log_2N\rvert\le C_\star\log\log N\). Proposition 14.2 is assembled uniformly for all masks in the critical window: \(d_{\rm TV}(\mathcal L(Z_{N,L}(A_N)),\mathrm{Pois}(|A_N|2^{-L}))=o_C(1)\). Its canonical endpoint assumes exactly AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin; no `internal/open` bridge reaches its signature |
| Th. 1.2(ii–iii) | Spatial and marked PPPs | **Proved as a complete characterization by Laplace functionals, modulo the literature only.** Expectations under the infinite law converge to the functionals of \(\operatorname{PPP}(\lambda\,dt)\) and \(\operatorname{PPP}(\lambda\,dt\otimes\nu)\), where \(\nu(\{e\})=2^{-(e+1)}\), for every nonnegative continuous test function with compact support in the marks. Uniform tightness is proved through \(\limsup_N\mathbb P(\text{mark}>E)\le\lambda2^{-(E+1)}\). Because mathlib does not yet provide a suitable API for point measures and the vague topology, a `Tendsto` formulation for the process laws is not duplicated artificially |
| Th. 1.4 | Moments and the homogeneous sum | **Conditionally proved, modulo the literature only.** `theorem_one_four_canonical` connects the finite kernel of §12 directly to the quantitative master mass bound and establishes, in the critical window, the first-moment rate, the little-oh estimate for the second factorial moment, \(R_2(N,L)=o_C(N^2)\), and the little-oh estimate for the variance. Its only assumptions are Evertse–Silverman, Halter–Koch, and Nicolas–Robin (`external`). The three legacy endpoints removed in v041 have not been reintroduced |
| Lem. 2.1 | Affine system, compatibility, and fiber size | **Proved in the finite affine model**: integral Fourier identity, characterization of relations, normalization by \(\eta 2^\rho\), compatibility, exact fiber count, rational probability, and the bound \(\lvert\eta2^\rho-1\rvert\le2^\rho-1\) |
| Lem. 2.2 | Relations, even subsets of a tree, and square products | **Proved**: equivalence “sum of parities zero ⇔ square product,” additivity of the boundary, existence on every connected graph, and an existence-and-uniqueness bijection between subsets of a tree's edges and even subsets of its vertices |
| Lem. 3.1 | Quantitative Runge | **Proved**, with no added axiom and with the explicit absolute constant \(C_0=128\): power-series expansion and convergence, selection of the positive branch, (3.2), the complete estimate (3.3), dyadic separation, construction and nonvanishing of \(Q\), vanishing of the high coefficients yielding \(\deg Q\le k-1\), uniform height, and the Cauchy bound. The final statement is \(U\le(128\,dR)^{2d}\) for \(d=2k\ge2\), under the exact hypotheses of the manuscript |
| Prop. 3.2 | Weighted defect mass | **Proved.** In addition to the full finite rank--Runge--Hamming chain, Chebyshev's inequality, weighted counting, the Euler product, and double counting, Lean now certifies the uniform choice \(T=\lceil\log N\rceil\), the noncircular radius budget, all eventual thresholds under \(c_1\log N\le H\le c_2\log N\), the pointwise bound for every \([U,U+H]\subset[N/2,3N]\), and (3.4). The notation is replaced by `UniformBigOOn` and the explicit condition \(\lvert M(N,H)\rvert^{2k}\le N^{k+1}\), which defines \(N^{1/2+o(1)}\) |
| Cor. 3.3 | First moment | **Proved in the finite affine model.** The relation \(\rho(x)\) is injected into the coordinates of the \((L+1)\)-defective vertices through the inner boundary of the start tree and the private primes. The exact connection to the mass of Proposition 3.2 then gives, uniformly under \(\lvert L-\log_2N\rvert\le C\), \(\mathbb E Z_{N,L}-N2^{-L}=N^{-1/2+o_C(1)}\), with explicit quantifiers |
| Lem. 3.4(i) | Incompatible overlapping starts | **Proved** |
| Lem. 3.4(ii) | Touching pairs | **Proved.** The joint system for two starts has \(2L\) rows, and its tree has an injective inner boundary, including at the shared vertex. Lean bounds \(\rho(x,x+L)\) by the \(2L\)-defects of \([x,x+2L]\), counts at most \(2N\) ordered pairs, transports Proposition 3.2 under \(\lvert L-\log_2N\rvert\le C\), and then obtains uniformly \(\sum_{|x-y|=L}(2^{\rho(x,y)}-1)\le N^{1+o_C(1)}\). The exact joint probability \(\eta2^\rho/2^{2L}\) and its error bound are also proved in the finite cylinder |

## Sections 4–8

| Manuscript | Content | Status |
|---|---|---|
| Lem. 4.2 | Number of relational hosts | **Proved.** Lean defines exactly the separated ordered pairs \((x,y)\in I_N^2\) with positive relational defect, canonically selects a relation and its first occurrence, and then assigns every prime \(p>L+1\) of odd valuation to the unique selected occurrence in the opposite block. Regrouping by assignments and applying the CRT give \(\displaystyle H_2(N,L)\le8(L+1)N\sum_{1\le n\le3N}(L+1)^{\omega(K_{L+1}(n))}/K_{L+1}(n)\). The canonical decomposition \(n=a^2ur\) and two elementary Euler products then close the finite bound \(\displaystyle H_2(N,L)\le8(L+1)N\sqrt{3N}\,e^{4\sqrt{L+1}}\), followed by its uniformly quantified formulation \(N^{3/2+o_C(1)}\), first under \(L+1\le C\log N\), and then in the exact window \(\lvert L-\log_2N\rvert\le C\) |
| Lem. 4.3 | Cauchy–Schwarz interpolation | **Proved.** For every finite subfamily of relational hosts and every nonnegative real weight, Lean proves \(\sum w\le\sqrt{H_2}\sqrt{\sum w^2}\), and then a version in which \(H_2\) and the quadratic sum are replaced by separate upper bounds. The specific calculation in (4.3) is certified: the exponents \(3/2+\varepsilon\) and \(5/2-\delta+\varepsilon\) yield \(2-\delta/2+\varepsilon\). An eventual uniform interface is also provided |
| Lem. 5.1–5.2 | Rational code and channel uniqueness | **Proved.** Lean identifies the full boundary with the zero-sum vectors, literally encodes even subsets of the exact units, injects them into \(R(x,y)\), computes \(\dim S_{a,b,h}=m-1\), and certifies the character \(d_{i,j}=1_{i=-1}+1_{j=-1}\), the criterion \(S_{a,b,h}\ne0\iff m\ge2\), and the bound \(a,b\le L\). The finset of reduced ratios, the optional canonical channel, \(S_{\rm rat}\), \(\sigma\), and \(\tau\) are defined. Uniqueness by determinant and its uniform version in \(\lvert L-\log_2N\rvert\le C\) are proved, as is coverage of every nonzero rational code. In the branch \(m_{\rm ex}\le1\), the definitions give \(S_{\rm rat}=0,\sigma=0,\tau=\rho\) |
| Prop. 5.4 | Systematic rational mass | **Proved.** The separated pairs, their canonical \(\sigma\), and the two masses \(\sum(2^\sigma-1)\), \(\sum(4^\sigma-1)\) are finite sums. Lean handles \(q=1\) by exclusion, \(q=2\) through two rectangles of cardinality \(O(B^2)\), and \(q\ge3\) through the explicit counts \(2q\), \((a+b)L+1\), \(1+N/q\), and \(\sigma\le L/q\). The resulting finite envelope yields the uniformly quantified predicates \(N^{4/3+o_C(1)}\) and \(N^{5/3+o_C(1)}\) |
| Lem. 5.5 | Weighted channel sum | **Proved.** The sum over geometries \((a,b,h)\) of multiplicity at least two is implemented by an exact finset; its finite bound is compressed to \(46(L+1)^4\,4^{\lfloor L/2\rfloor}\), after which the critical window gives \(N^{1+o_C(1)}\) uniformly |
| Lem. 6.1 | Large-prime graph and solution of \(W_{>B}\) | **Fully proved.** For \(p>L+1\), there is at most one odd occurrence per block and at most two in total; cardinalities two and one give, respectively, a bipartite edge and a pin. The equations of \(W_{>B}(x,y)\) say exactly “constant along edges, zero at pins.” Lean constructs the canonical linear equivalence with functions on the unpinned components, proves \(\dim W_{>B}=D+c\), identifies \(D\) with the number of defective vertices, and establishes \(D+2c\le2(L+1)\) |
| Lem. 6.2 | Square class of an unpinned component | **Square-class conclusion proved.** Under \(x,y\ge2\), \(L<\operatorname{dist}(x,y)\), and for an unpinned component, the sum of parities is zero at every prime \(p>L+1\). The product of its labels is therefore \(d z^2\), where \(d>0\) is squarefree, every prime dividing \(d\) is at most \(L+1\), and \(z>0\). The factor \(d\) and the positive root are canonical and unique |
| Lem. 6.3 | Exact-unit dichotomy and separation | **Fully proved under its explicit hypotheses.** For every exact unit equipped with its primitive factorization and satisfying \(\max(a,b)<L+1\), the two occurrences have the same large odd kernel. If that kernel is one, both vertices are defective; otherwise they are adjacent and form an unpinned component of cardinality exactly two. For two distinct units in the same positive channel, under coprimality, height identity, and cutoff hypotheses, Lean also proves that their pairs of occurrences are disjoint and their components distinct; in the nondefective branches, the two components are distinct, unpinned, nontrivial, and of cardinality two |
| Lem. 6.4 | Quotient core and corrected counts | **Proved.** Lean literally identifies \(\tau=\dim(R/S_{\rm rat})\), injects the boundary of \(R\) into \(W_{>B}\), formalizes the loss of one dimension through left parity, splits the \(m\) units into \(d+n\), defines \(D^\#,c^\#\) with the \(m\ge2\) branch, and then proves \(\tau\le D^\#+c^\#\) and \(D^\#+2c^\#\le2B\). An injection of defective vertices into the two interval-defect sets, followed by Proposition 3.2 and a uniform comparison of logarithms, gives literally \(D^\#\ll_C B/\log B\) in the critical window under the explicit geometric hypotheses |
| Lem. 6.5 | Canonical extraction of residual certificates | **Proved.** The concrete family \(C_{\rm res}\) is defined and has cardinality \(c^\#\). Each component canonically chooses its least prime and then its lexicographically least cell. The certificate family has cardinality \(c^\#\); left offsets, right offsets, and primes are injective, \(p_s>B\), \(p_s\mid x+i_s,y+j_s\), and the cells are not aligned when \(m\ge2\). For \(m=1\), every exception belongs to a family containing at most two components |
| Lem. 7.1 | Summation of CRT certificates | **Proved in its exact finite formulation.** Lean handles cells labeled by actual primes, requires them to be distinct, deduces coprimality, connects \(P\le N\) to the counts \((C_0+1)N/P\) and \(((C_0+1)N/P)^2\), sums unordered certificates with the exact factor \(1/r!\), groups the cells into \(u\sum_pE_p/p\) and \(uM\sum_p1/p^2\), and then bounds the truncated series from above by the exponential. The canonical residual certificate of size \(c^\#\) is constructed; its product is \(P^\#\), its admissibility is equivalent to \(P^\#\le N\), and \(x,y\) satisfy all its congruences. The global regrouping of pairs by cells is now certified in Proposition 7.3 |
| Lem. 7.2 | Cells in a channel | **Proved in an effective finite formulation.** Lean defines \(\Delta=h+bj-ai\) and the cells \(p\mid\Delta\ne0\), proves \(\lvert\Delta\rvert\le2qL<4q(L+1)\), hence \(p<4qB\), partitions by the values of \(\Delta\), counts those values modulo \(p\), and then bounds each fiber by a translated channel. It obtains exactly \(E_p\le(1+8qB/p)(1+B/q)\). Dyadic shells derived from \(\lfloor\log_2H\rfloor\pi(H)\le7H\) yield \(\sum1/p\le14K/\lfloor\log_2B\rfloor\) and \(\sum1/p^2\le28/(B\lfloor\log_2B\rfloor)\). Under \(2\le q\le L\), \(B=L+1\ge4\), the full mass of the residual support satisfies \(\sum E_p/p\le896B/\lfloor\log_2B\rfloor\). Lean also proves that every prime cutoff containing \(B2^{q+2}\) gives exactly the same sum, since the additional terms vanish |
| Prop. 7.3 | Sector \(P^\#\le N\) | **Fully proved under the explicit hypotheses.** The literal masses \(Q_{\rm res}\) and \(R_{\rm res}\) are defined, and the partition into \(\sigma=0\)/\(\sigma>0\) is exact. The first branch is covered by the two-dimensional CRT certificates of Lemma 7.1; the second is grouped without double counting by the keys \((q,a,b,h,r)\), and then connected to Lemmas 5.5 and 7.2. The finite maximum of \(D^\#\) uniformly absorbs the factor \(4^{D^\#}\). Lean obtains \(Q_{\rm res}\le N^{2+o_C(1)}\), certifies the interpolation inequality (6.6), and then deduces \(R_{\rm res}\le N^{7/4+o_C(1)}\) in the critical window |
| Prop. 7.4 | Sector \(P^\#>N\), \(q\le\sqrt{\log B}\) | **Fully proved under the explicit hypotheses.** The population retains the literal real-valued criterion and a witness for the canonical channel. Lean bounds \(c^\#\) by \(2+\pi(4qB)\), uniformly deduces \(c^\#=o(B)\) and then \(4^{D^\#+c^\#}=N^{o_C(1)}\), separates \(\sigma=0\) and \(\sigma>0\) exactly, and obtains \(Q_{\rm res}\le N^{5/3+o_C(1)}\). Finite interpolation gives \(R_{\rm res}\le N^{19/12+o_C(1)}\), and then \(R_{\rm res}=o_C(N^2)\) |
| Prop. 7.5 | Remaining moderate sector | **Fully proved under the explicit hypotheses.** Lean defines exactly the separated pairs with \(P^\#>N\), outside the canonical channel of height \(q\le\sqrt{\log B}\), and imposes the rounding-unambiguous condition \(16c^\#\le3B\). After removing the \(\tau=0\) terms, the active population is contained in the relational hosts. The maximum of \(\sigma\) is uniformly \(o(B)\), because \(\sigma>0\) exposes the canonical candidate, \(q\sigma\le L\), and the complement of the small-height regime forces \(q>\sqrt{\log B}\). With \(4^{D^\#}=N^{o_C(1)}\) and \(4^{\lfloor3B/16\rfloor}=N^{3/8+o_C(1)}\), Lean obtains \(Q_{\rm res}\le N^{19/8+o_C(1)}\). Interpolation against \(H_2\le N^{3/2+o_C(1)}\) gives \(R_{\rm res}\le N^{31/16+o_C(1)}=o_C(N^2)\). The exact partition among Sectors 7.3, 7.4, 7.5, and the remaining deep core is also proved |
| Th. 8.1 | Exclusion of the aligned core | **The instance \(\alpha=3/16\) required by the partition is fully proved.** Lean removes at most two components meeting an exact unit, extracts at least \(B/16\) exact-free components of size \(\le43\), constructs the matrix \(\Phi\) and its two parities, obtains a short codeword, proves the square-product identity and the distinctness of the roots, and then applies Runge. The integer radius \(\lceil64B/(\lfloor\log_2B\rfloor\lfloor\log_2\lfloor\log_2B\rfloor\rfloor)\rceil\) eventually satisfies all Hamming inequalities uniformly; the bounds \(R=3H B\), \(2R\le ax\), and the Runge growth condition are closed for \(H\le B^A\). The final theorem `no_aligned_deep_core_eventually` directly excludes \(3B<16c^\#\) in the critical window. Only the literal generalization of the constants to every real \(\alpha>0\) remains to be encoded |

## Sections 9–12

| Manuscript | Content | Status |
|---|---|---|
| Lem. 9.1 | Evertse–Silverman | **External bridge.** The specialized count of abscissas \(X\) for which \(Z^2=e\prod_r(X+h_r)\) has a solution \(Z\ne0\), with its exact set of bad places and the upper bound \(7^{4+9|S|}\), is encoded as an explicit hypothesis. Lean **proves** that there are at most two ordinates per abscissa, obtains the upper bound \(2\cdot7^{4+9|S|}\) from (9.2), counts at most \(d\) zero solutions, and then transports the bound to \(\prod_r(X+h_r)=eY^2\) through the injection \((X,Y)\mapsto(X,eY)\). The cited result itself is not declared as a Lean theorem |
| Lem. 9.2 / 17.19 | Uniform Pell | **Internal bridge discharged; Lean proof conditional on two narrow external inputs, for every \(K_0\in\mathbb R_{>0}\).** Lean formalizes \(x+y\sqrt D\), its principal ideal dividing \((M)\), unit orbits, height control, and squarefree reduction; `QuadraticIdealDivisors` proves internally the \(\tau(|M|)^2\) ideal-divisor bound. The Halter–Koch boundary cites Th. 1.1.6(1)(b), pp. 4–5; Def. 5.1.6 and Th. 5.1.7(1),(3), pp. 118–119; Th. 5.2.3(1),(2), p. 125; and Th. 5.2.5(1), pp. 126–127. These results are intended to supply the conductor and unit-coset facts, but the rc2 counter-audit has **not established** the complete source-to-record construction. `HalterKochConductorDescent` derives the historical `Fin 4` interface only after the essential conductor-descent data are assumed; the order embedding and unit-quotient instantiation remain the registered external premise. Starting from Nicolas–Robin, Lean proves the remaining divisor-envelope and polynomial substitutions; `PCv07c-L9.2-generalized-Pell` remains discharged |
| Lem. 9.3–9.8 | Component counts | Lemma 9.3 **proved**: canonical squarefree normalization, uniqueness, and bounds. Lemma 9.4: degree-one count **proved** and injective reductions to Pell and Evertse–Silverman **conditionally proved**; their uniform sums are now assembled in 17.26 and 17.28. Lemma 9.5: canonical parametrization, converse, uniqueness, bounds, and the divisor sum needed in degree two **proved**. Lemmas 9.6–9.7: structural extraction of a small component and disintegrations by forms **proved**; the global connection specific to 17.28 is closed. The former generic host count from 9.9 is no longer exposed as an interface. Lemma 9.8: distinct-class reductions to Pell, equal-class factorization, and smooth summations required by the bounded-ratio sectors **conditionally proved under Pell** |
| Prop. 9.9 | Elimination of \(D^\#\ge3\) | **Finite kernel retained; former conditional endpoint removed.** Lean defines the exact population \(\{\text{deep core},\sigma=0,D^\#\ge3\}\), proves the inequality \(D^\#+c^\#\le B+\lfloor D^\#/2\rfloor\), the finite weight and mass envelopes, removes zero-mass pairs, and covers the active hosts by two oriented branches carrying a canonical component whose support size lies between 2 and 10. Version v041 removes the abstract host-count interface and the public theorem that depended on it; the canonical master mass bound proceeds through the direct bounded-ratio counts |
| Lem. 9.10 | Exact rank formula | **Proved in its non-aligned source scope.** Lean constructs the corrected defective vertices, the genuine residual components, and the canonical matrix with \(\pi(L+1)+2\) rows. When the canonical selector is `none`, the \(D^\#+c^\#\) coordinates parametrize the entire large-prime solution space. Under \(L+1\le M\), \(x+L\le M\), and \(y+L\le M\), the full boundaries yield a linear equivalence between the relation space and the concrete kernel. Because the canonical rational code vanishes, Lean obtains the residual-quotient--kernel equivalence and \(\tau+\widetilde k=D^\#+c^\#\), without a bridge. The historical interface `CanonicalArithmeticKernelStatement` is retained with `status: discharged` |
| Prop. 9.11 | Terminal reduction | **Canonical conclusion proved; arbitrary interface removed.** For \(s=B-c^\#\), the concrete residual family contains at most \(2s\) components of size at least three and at least \(B-3s\) components of size two. The finite integer-budget proxy and the bound \(\tau\le B+2\) are retained. On the canonical route, `BoundedRatioNonterminalAssembly` chooses the threshold and proves the smallness of the excluded mass under Evertse–Silverman and Pell. Version v041 no longer retains the generic interface parametrized by an arbitrary rank function and budget |
| Lem. 9.12 | Kernels and determinants | **Arithmetic kernel proved**: equality and nontriviality of the kernel within every size-two component, coprimality across components, divisibility and nonvanishing of the determinant under non-alignment, with the explicit bound \(\lvert\Delta\rvert\le6N(L+1)\) |
| Th. 10.1 | Terminal closure | **Canonical instance proved; generic wrapper removed.** Lean certifies uniqueness above the threshold, the double counts, the weighted sum, the injective reduction of a partner to Pell, the exact bound on \(A_{B,T}(X)\), terminal disintegration, and the “first starts × partners” upper bound. The canonical chain also assembles \(\#T_K\le N^{3/4+o(1)}\) and the mass \(N^{7/4+o(1)}\) under the now-discharged Pell bridge. Version v041 removes the former endpoint for arbitrary `smallRowRank/rankBudget` families |
| Lem. 11.1 | Exhaustive partition | **Proved for the six ordered tests and the five canonical arithmetic tests.** The seven populations cover all separated pairs, are pairwise disjoint, and determine a unique sector; Sectors 1–3 are exactly those of Section 7, and the union of Sectors 4–7 is the deep core. The generic integer-budget terminal test remains defined; in the bounded-ratio proofs, the canonical route directly instantiates the intrinsic population and its threshold |
| Prop. 11.2, Cor. 11.3 | Absolute homogeneous sum, quantitative form | **Canonical exponential endpoint proved modulo the literature.** `DyadicKappaTransport` proves \(R_2(N,L)=R_{2,\kappa}(N,2N,L)\), and `DyadicKappaQuantitative` combines the rates \(3/2\), \(7/4\), \(31/16\), and \(5/3\) with the dense-sector saving across all seven sectors. The public theorem `corollary_eleven_three_canonical` chooses a uniform \(c_R>0\) and proves exactly \(R_2(N,L)\ll_C N^2\exp(-c_R\sqrt{\log N}/\log\log N)\) under Evertse–Silverman, Halter–Koch, and Nicolas–Robin. The harmonic \((\log\log N)^{-2}\) consequence used by Corollary 13.10 remains available. Version v041's abstract legacy endpoints and internal bridges remain removed |
| §12 | Deduction of moments | **Proved and canonically connected; legacy endpoints removed.** Off-diagonal pairs are partitioned exactly into strict overlap, contact, and separation; Lean retains the second-factorial-moment identity, the finite bounds, and the error identities for the second moment and the variance. Version v042 connects these components to the quantitative master mass bound in `theorem_one_four_canonical`. The three public conclusions that accepted the generic interfaces from 9.9, 9.11, and 10.1 remain removed |

## Sections 13–14

| Manuscript | Content | Status |
|---|---|---|
| Lem. 13.1 | Private pivots | **Proved**: abstract linear criterion, specialization to the edges of a tree, and effective projection onto the prime coordinates \(p>Y\), under explicit arithmetic hypotheses |
| Cor. 13.2 | Exact conditional marginal | **Proved exactly in the finite cylinder.** Lean splits the prime coordinates into \(p\le Y\) and \(p>Y\), reconstructs every assignment from these two parts, translates the right-hand side of the system after an arbitrary fixing of the small primes, and then uses the private pivots to make the large part surjective outside \(D_Y\). For every fixed assignment \(\sigma\), it proves \(\#\mathrm{solutions}\,2^L=\#\mathrm{large\ assignments}\), and hence the exact uniform probability \(2^{-L}\). Conditioning is represented extensionally by the uniform law on the remaining cylinder, without a measure-theoretic API for conditional probability |
| Lem. 13.3–13.6 | Bad starts and the conditional graph | **Proved in the critical window and the finite cylinder.** For 13.3, Lean injects incidences, encodes \(Y=\lfloor B^2\log B\rfloor\), establishes the explicit finite envelope, and then proves \(\#D_Y=N^{1/2+o_C(1)}\), \(\#D_Y=o_C(N)\), and \(2^{-L}\#D_Y=o_C(1)\), without a bridge. For 13.4, it connects `terminalDefectWeightMass` exactly to the mass in Proposition 3.2 and concludes that \(\sum_{x\in D_Y}\mathbb P(J_x)=o_C(1)\). For 13.5, the supports \(\Pi_x(Y)\), the simple graph, extensional dependence, and the uniform law on the remaining cylinder are **proved**; the factorization now covers every Boolean assignment to an arbitrary family of nonneighbors and directly instantiates `HasExactDependencyGraph`. For 13.6, the explicit upper bound and \(E_Y=o_C(N^2)\) are **proved without a bridge**. Only the finer quantitative formula in 13.6 remains outside this formalization line |
| Th. 13.7 | Arratia–Goldstein–Gordon | **External bridge registered.** Lean defines the finite probability space, the indicators, the exact dependency graph, \(b_1,b_2\), the law of the sum, the Poisson law, and total variation; the consequence \(d_{\rm TV}\le2(b_1+b_2)\) explicitly assumes `ArratiaGoldsteinGordonStatement` |
| Lem. 13.8, Cor. 13.9 | Chen–Stein terms | **Finite kernels proved; legacy public route removed.** Lean establishes the finite identity and upper bound for \(b_1\), the exact partition of \(b_2\), and then the little-oh estimates from a supplied homogeneous estimate. The AGG application, total law, exact expectations of \(b_1,b_2\), coupling of the full count with the good count, the inequality \(d_{\rm TV}(\mathrm{Pois}(r),\mathrm{Pois}(s))\le|r-s|\), and the shift to \(N2^{-L}\) remain formalized. Version v041 removes the qualitative endpoints that reconstructed the homogeneous estimate from the three former internal interfaces; Corollary 13.9 now follows from the quantitative canonical endpoint 13.10 |
| Cor. 13.10 | Explicit rate | **Conditionally proved, modulo the literature only.** Lean proves the uniform rate \(d_{\rm TV}(\mathcal L(Z_{N,L}),\operatorname{Pois}(N2^{-L}))\ll_C(\log\log N)^{-2}\) in the exact critical window: the master mass bound from the \(κ\)-proofs, the edge term at the terminal cutoff, the two Chen–Stein terms, the mass and normalized cardinality of bad starts, and finally the TV triangle inequality. `corollary_thirteen_ten_uniformBigO_canonical` assumes exactly AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin (`external`). The former sectorwise and direct-Pell variants were removed in v041 |
| Prop. 14.1–14.2 | Masks | Proposition 14.1 is **proved uniformly for all masks in the exact window** \(\lvert L-\log_2N\rvert\le C_\star\log\log N\), without a bridge. Proposition 14.2 is **proved modulo the literature only**: the exact graph after masking, \(b_1,b_2\), the coupling, the parameter shift, and the uniform TV little-oh estimate are assembled through the quantitative \(κ\) master mass bound. The canonical endpoint exposes AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin, but no open internal bridge |
| Rem. 14.3 | Uniform masked rate | **Conditionally proved, modulo the literature only.** `maskedPoissonTotalVariation_uniformBigO_canonical` dominates the induced-subgraph \(b_1,b_2\) terms term by term before averaging, and controls the removal and parameter-shift corrections by the full-block positive envelopes. Uniformly over every deterministic \(A_N\subseteq I_N\), it obtains \(d_{\rm TV}(\mathcal L(Z_{N,L}(A_N)),\operatorname{Pois}(|A_N|2^{-L}))\ll_C(\log\log N)^{-2}\). Its four open inputs are AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin |
| Lem. 14.5 | Almost-sure finiteness of constant stretches | **Proved in the source infinite model.** Lean constructs the Rademacher product measure on the prime coordinates, proves that a prescribed tail of bits has measure zero, and simultaneously deduces, for every \(x\ge2\), the almost-sure existence of \(n\ge x\) such that \(f(n)\ne f(x)\). Its projection onto every finite cylinder is exactly the preceding uniform law. Exact-length events are measurable, and their infinite measure is identified exactly with their rational probability on every cylinder covering their support. The following connection is also proved: almost surely, a start has a unique excess \(e\), with the set of active marks having cardinality \(1\), versus \(0\) outside the event |
| Lem. 14.6 | Mixed lengths | **Proved in the finite cylinder.** For \(q_e=L+e+1\) and \(q_f=L+f+1\), Lean constructs the stacked affine system for the two exact-length events, identifies its fiber exactly with their intersection, and obtains \(2^{-q_e-q_f}\eta_{e,f}2^{\rho_{e,f}}\). For \(e,f\le E\), zero-extension of the row coefficients is a linear injection into the common system of length \(Q=L+E+1\), yielding \(\rho_{e,f}\le\rho_Q\), without a bridge |
| Lem. 14.7 | Cycle rank | **Proved** for a finite oriented presentation allowing loops and parallel edges: dependencies in the cycle space, the safe truncated bound \(\dim\le |E|-(|V|-|C|)\) under explicit connectivity to the roots, and then the rank bound \(|V|-|C|\) |
| Lem. 14.8 | Removed-start mass, exact lengths | **Proved under the source infinite law, through the uniform little-oh estimate.** After fixing the small primes, Lean constructs the exact systems and their probabilities \(\eta2^\rho/2^m\). The direct count of nonroot defects separates defective supports from full-rank systems, gives a finite bound with a harmless factor \(2\), and then aggregates the \(E+1\) lengths. The already certified weighted-mass and bad-start cardinality estimates are transported from \(L\) to \(Q=L+E+1\). `lemma_fourteen_seven_finiteCylinder` gives the little-oh estimate for the finite double rational sum; the exact identity in `InfiniteExactLengthProbabilityTransfer` transports it term by term, and `lemma_fourteen_seven` gives literally \(\sum_{e\le E}\sum_{x\in D(Q)}\mathbb P_\infty(K_{x,e}=1)=o_{C,E}(1)\), with no new hypothesis |
| §14.2, §14.4 | Spatial and marked processes | **Proved in Laplace-functional form.** Independent thinning, the void identity, exact dependency graphs, the terms \(b_1,b_2\), local ranks, local/far separation, Riemann limits, cylinder-to-source transfers, and bad-start corrections are assembled. The canonical route gives both PPP functionals under AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin. `FullMarkedLaplaceTransfer` defines the complete marked source functional with a sum over all excesses and proves, for every test function with finite support in the marks, its pointwise equality and then equality in expectation with the truncated version. The public endpoint literally concerns this complete expectation; detruncation additionally gives uniform tightness of the marks. This family of functionals is the formal endpoint for PPP convergence in the absence of a vague topology for point processes in mathlib |
| Cor. 14.9 | Exact counts and the maximum | **Canonically proved, modulo the literature only.** For every \(m\), Lean concludes \(\mathbb P(M_{N,L}\le m)\to\exp(-\lambda2^{-(m+1)})\). For each cutoff \(E\), the source vector, its retained version, the conditional mixture, and their coupling error are identified exactly. The encoding by powers of primes is injective; `PrimeEncodedCountLaplace` identifies the inverse transforms of the source law with the marked tests \(s\log p_e\), `PoissonVectorMass` computes those of the product-Poisson target, and `tendsto_atoms_of_inversePowerTransforms` deduces convergence of every joint mass. `corollary_fourteen_eight_counts` assumes exactly AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin, with no retained-law premise and no internal bridge |

## Sections 15–17

| Manuscript | Content | Status |
|---|---|---|
| Lem. 15.1 | Low zone | **Conditionally proved under a single source-level external bridge, with a complete finite kernel.** Lean constructs the intermediate primes and their disjoint supports, an explicit square outside their union, and proves independence of the forms on the even hyperplane, surjectivity, \(\rho\le L-r(x)\), \(\mathbb P(J_{x,L}=1)\le2^{-r(x)}\), the identity \(r(x)=\pi(L)-\pi(\sqrt{x+L})\), and the summed bound. The passage from \(x\le L^{2-\varepsilon}\) to the integer gap, the monotonicity of the envelope, and the uniform lower bound \(3\log_2L\le\pi(L)-\pi(\sqrt{x+L})\) are **proved**. The sole `external` bridge is now the directly sourced prime-number-theorem statement \(\pi(t)\log t/t\to1\) |
| Lem. 15.2 | Laishram–Shorey | **Conditionally proved under two external bridges.** The published Corollary 1 is transcribed with its exact minimum, and PNT supplies \(c_3=1/32\). The entire finite reduction is proved; Lean then sums the probabilities over the exact band \(L+2<x\le2L^2\) and obtains an envelope tending to zero |
| Lem. 15.3 | Rarity of two defects | **Conditionally proved, uniformly for \(2L^2\le N\le M\).** Lean counts squarefree \(B\)-smooth kernels, constructs the two canonical decompositions, excludes equal kernels in the high zone, and closes \(\#\{x\in[N,2N):m_x\ge2\}\le\exp(K_C L/\log L)\), with the same constant and threshold when \(L\) is critical for \(M\). The direct interface assumes PNT and Pell; the latter is now `discharged` from Halter–Koch and Nicolas–Robin |
| Lem. 15.4 | Balasubramanian–Shorey | **Conditionally proved under one registered external bridge.** The source statement BS93 is an ordinary hypothesis. Lean constructs \(by^2\) from the product of the defective vertices, proves \(P^+(b)\le B\), and then eventually obtains \(m_x<\mu_B(\theta_0)\) and the literal form \(m_x\le B-g_B\), \(g_B=B-\mu_B(\theta_0)\), for \(x>B^2+2\) |
| Prop. 15.5 | Truncation of the deep blocks | **Conditionally proved.** Lean defines the literal mass of deep starts, completely covers the low zones by 15.1–15.2, combines on each block the uniform rarity from 15.3 with the probability saving from 15.4, and then constructs the global dyadic partition without gaps or overlaps. The last block is dominated by its full envelope, and the number of blocks is bounded by \(2L\). Exceptional decay and the geometric tail give \(O_C(2^{-j_0})+o_M(1)\), hence the double limit in the exact order of the manuscript. The direct interface assumes PNT, Laishram–Shorey, Balasubramanian–Shorey, and Pell; the internal Pell bridge is now `discharged` |
| Prop. 16.1 | Bounded-ratio sum | **Conditionally proved, modulo the literature only in the canonical API.** Lean literally encodes \(U(N,M)=[N,M)\), \(2N\le M\le\kappa_0N\), defines \(R_{2,\kappa}\), proves the systematic/residual decomposition and the exhaustive partition into seven sectors, and then assembles the uniform little-oh estimate. The volumetric channel \(q=2\), Sectors 17.14–17.16, and the \(\alpha=3/16\) instance of 17.17 are **proved without a bridge**. Lemmas 17.26, 17.28, and 17.30 are closed under Evertse–Silverman and the now-discharged Pell interface: degrees \(1,2,\ge3\), the two-singleton envelope, the moving branch, the choice of \(K\), the terminal population, and summation are assembled. The direct generic assembly retains the three historical `discharged` interfaces; its three intermediate adapters are removed. The canonical API exposes only Evertse–Silverman, Halter–Koch, and Nicolas–Robin |
| Th. 16.2 | Interior global law | **Conditionally proved on both the finite cylinder and the infinite source law, for \(C>0\), modulo the literature only.** Lean defines the global and retained counts, proves their couplings and the double passage in the order \(j_0\) then \(M\), and first obtains the exact-mean law at \(\Lambda_M\). `theorem_sixteen_two_recentered_canonical` then uses \(|\Lambda_M-M2^{-L}|=o_C(1)\) and Poisson coupling to prove directly \(d_{\rm TV}(\mathcal L(Z_M),\operatorname{Pois}(M2^{-L}))=o_C(1)\) and \(\mathbb P(Z_M=0)=e^{-M2^{-L}}+o_C(1)\). Exact cylinder transfer defines `infiniteGlobalStartCount`, identifies its law with `globalStartLaw`, and exposes `theorem_sixteen_two_infinite_model` with the same seven external inputs. The main canonical variants assume neither a threshold \(K\), nor a terminal family, nor a Section 17 estimate, nor an internal bridge: their only unformalized premises are PNT, Laishram–Shorey, Balasubramanian–Shorey, AGG, Evertse–Silverman, Halter–Koch, and Nicolas–Robin (`external`) |
| Cor. 16.4 | Finite-prefix law | **Conditionally proved on the infinite source law, modulo the same seven literature inputs.** Lean defines \(W_{M,L}=\mathbf1_{\{f(1)=\cdots=f(L)\}}+\sum_{2\le x\le M-L+1}J_{x,L}\) and the longest prefix stretch \(R_M\), then proves pointwise that \(W_{M,L}=0\iff R_M<L\). Exact cylinder transfer identifies the source law. The left boundary is exactly the zero assignment on the primes at most \(L\), hence has mass \(2^{-\pi(L)}\). The starts with \(M-L+2\le x<M\) form a deterministic mask in the dyadic block based at \(M-L+2\); Proposition 14.1, the transported critical window, and the \(O(L2^{-L})\) baseline make their total mass uniformly \(o_C(1)\). `corollary_prefix_law_canonical` combines this coupling with the recentered Theorem 16.2 to conclude \(d_{\rm TV}(\mathcal L(W_{M,L}),\operatorname{Pois}(M2^{-L}))=o_C(1)\) and \(\mathbb P(R_M<L)=e^{-M2^{-L}}+o_C(1)\), with no new bridge |
| Lem. 17.1–17.37 | Bounded-ratio transport | **Conditionally proved through the complete assembly.** The finite geometry, rational and residual masses, weight identity, and seven-sector partition are certified. Lemmas 17.14–17.16 are **proved**, as is the \(\alpha=3/16\) instance of 17.17; the density geometry is additionally parametrized for every lower rational density and every admissible cutoff. Lemma 17.26 is closed under Evertse–Silverman and Pell by the uniform sums for degrees \(1,2,\ge3\). Lemma 17.28 is closed under the same bridges by the moving count and the two-singleton count, with an internal choice of \(K\). Lemma 17.30 is closed under Pell and the source-exact Lean theorem from 9.10. Lemmas 17.33–17.37, their bad starts, \(b_1\), the expectation of \(b_2\), the couplings, and transport to the retained count are **proved**; the Chen–Stein application remains conditional on AGG and Proposition 16.1. The three historical sector interfaces remain registered with `status: discharged`, but none is consumed by the canonical assemblies |

## Encoding choices and remaining Lean obligations

1. Generalize the constants in the \(\alpha=3/16\) instance of Theorem 8.1
   to an arbitrary positive real parameter; the instance used by the
   Section 7 partition is fully closed.
2. Retain the historical interfaces 9.2, 9.10, 17.26, 17.28, and 17.30 with
   `status: discharged` for traceability; none is now required by the
   canonical APIs 1.1, 1.4, 13.10, 16.1, and 16.2.
3. Reserve the “full model” interpretation of `startProbability N L x` for
   windows whose vertices all lie below `dyadicCutoff N L`; this is the case
   for `x ∈ dyadicBlock N`. Outside this scope, the current definition is
   intentionally that of a truncated prime cylinder.
4. Explicitly carry the hypotheses \(n\ge1\), \(x\ge2\) from the paper's
   arithmetic statements whenever the generic APIs also permit zero.
