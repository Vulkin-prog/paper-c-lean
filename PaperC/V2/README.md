# Paper C v2 formalization line

This directory is an isolated development line based on the immutable v1
authority commit `b3cf107d2df629453a5da8e84f2bad29eea0bf94`.

It does not alter or reinterpret the existing Palomar records. Those records
remain evidence for the v1 common core only.

## Initial milestones

1. `SyndromeCollision.lean`: direct subset-syndrome proof replacing the two
   Hamming-ball presentations in the v2 manuscript.
2. A finite truncated-Rankin counting theorem for values with trivial large
   odd kernel.
3. The optimized cutoff `Y = floor(exp(sqrt(B)))` and the corresponding
   bad-start and dependency-edge asymptotic envelopes.
4. Assembly of the strengthened dyadic rate and its fixed bounded-ratio and
   deterministic-mask transports.
5. Separate Comparator, audit manifest, and Palomar candidates after the v2
   manuscript statements stabilize.

No file in this directory is part of a release record until a dedicated v2
qualification explicitly says so.
