# Absolute pair completion and divergence at higher orders

The [receipt](validation.json) binds **1,048 theorems in 215 modules** to exact
source hashes. This checkpoint adds **43 theorems in 13 modules**. All
**24 libraries build (10,330 Lake jobs)**. The exact **1,048-name axiom audit**
passes, using only `propext`, `Classical.choice` and `Quot.sound`.

G.7 is proved for the actual signed/excess indicators, every deterministic
subset of G0, both paper cutoffs, and every cylinder C>=M+Q. The host count
has exponent -2V with arbitrary positive second-scale slack. The paper budget
gives 4 exp(-I-d*nu)+2 M^(-1/3+epsilon), for every 0<d<2c, and convergence.
Exact double counting identifies this with the categorical pair layer. On the
G.9 prime windows, that layer vanishes while orders >=3 diverge, for G0 and
each fixed nonnegative theta. Only the existing PNT premise is used here.

Final unnumbered assertions and the prescribed-floor field specialization
remain. **Full realignment remains incomplete.** F.2 retains its explicit
analytic solution premise. No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payloads** are unchanged.
Ten regression tests, source/coverage inventories and repository guards pass.
Historical receipts are preserved; full compressed transcripts are included.
