# Actual value profile and information-dependent cutoff

The [receipt](validation.json) binds **204 proved theorems in 37 modules** to
exact source hashes. This batch adds 17 theorems. All **24 libraries build
(10,152 Lake jobs)** and the exact **204-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

The actual retained full-value relation excess is identified with the existing
arithmetic profile on the shifted start mask. Its coarse and normalized bounds
are proved and the former is substituted in the infinite conditional comparison.
The literal cutoff `ceil((I+V+log(2+lambda))/log 2)` has an exact tail budget,
uniform shifted-band admissibility and subpolynomial profile inflation.
The entire conditioned relation term is bounded by `M^(-1/3+epsilon)` under
the explicit ambient information budget and geometric hypotheses.

The paper uses `(M-L)*2^-L` in its budget and `M*2^-L` in the cutoff. Converting
the former budget to the latter is still required. Other geometry, error
assembly, final uniform limits and the other new families also remain open.
This is not the full theorem 7.7 or a claim of complete paper alignment.

All 1,171 baseline Lean files and all 28 manuscript files remain unchanged.
Ten regression tests, inventories and repository guards pass. Complete build
and axiom-audit transcripts are compressed with hashes of both representations.
The existing analytic Stein input remains explicit in the field comparison.

The [previous receipt](../arithmetic-ledger/README.md) remains historical and
source-bound. No Palomar or NanoDa/Comparator qualification is claimed.
