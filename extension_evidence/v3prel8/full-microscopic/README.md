# Full microscopic comparison with explicit analytic inputs

The [receipt](validation.json) binds **245 proved theorems in 51 modules** to
exact source hashes. This batch adds **41 theorems**. All **24 libraries build
(10,166 Lake jobs)** and the exact **245-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

The paper information budget now implies the ambient budget and all actual
geometry at the literal excess cutoff. Actual conditional source deletion and
mark tails, independent target costs, exact relabelling and countable-space
restoration combine into the full interior spatial field. Every position,
actual sign and unbounded excess is retained.

For every positive full-F_Y event, the comparison is bounded by
`10*exp(-c_prime*nu)+4*M^(-1/3+epsilon)`. The literal moving-depth normalization
of 7.7 is instantiated. Convergence follows for actual varying fields under the
explicit source regime. Every measurable readout contracts the distance.
The existing directional Stein, PNT, Laishram-Shorey and Nicolas-Robin inputs
remain explicit; F.2's analytic solution construction is not newly discharged.
No final comparison or convergence premise closes the proof.

The dyadic endpoint, arbitrary common Markov-kernel readouts and the other new
families remain incomplete. This is not complete 3PREL8 alignment.
All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
The [previous receipt](../value-profile/README.md) remains historical and bound
to its original sources. No Palomar or NanoDa/Comparator qualification is claimed.
