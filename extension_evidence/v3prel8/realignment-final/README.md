# Final mathematical realignment — 3PREL8

The [receipt](validation.json) binds **1,128 theorems in 233 modules** to exact
source hashes. The last batch adds **41 theorems in 9 modules**. All **24
libraries build (10,348 Lake jobs)**. The exact **1,128-name axiom audit** passes,
using only `propext`, `Classical.choice` and `Quot.sound`.

The literal ordinary outside-deletion probabilities converge to zero. Raw,
centered and reference-centered cumulant conventions are connected, including
nonzero singleton corrections. The concrete two-rank example has certified
factorizations, original arithmetic values, ranks and exact probabilities.
The complete changed-source review includes 96 numbered statements, 11 final
review groups and all 32 inherited unnumbered groups, including D.1/D.4.

Realignment is complete **relative to the explicit baseline inputs and proof
substitutions**. In particular, F.2's analytic Stein solution remains an input;
the printed semigroup construction is not reconstructed. Two intermediate
derivative estimates are replaced by monotonicity, not claimed as separately
proved formulas. Earlier fidelity limits remain in force. New Palomar and
Comparator/NanoDa qualification is subsequent release work.

All **1,171 baseline Lean files** and **28 manuscript payloads** are unchanged.
Ten regression tests, source/coverage inventories (including source-bound
unnumbered review), and repository guards pass. Historical receipts and full
compressed transcripts are preserved. The delivered PDFs and source closure
were checked; no local TeX rebuild is claimed.
