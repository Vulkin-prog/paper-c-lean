# G.2 completion and actual Poisson-size spatial clouds

The [receipt](validation.json) binds **658 proved theorems in 137 modules**
to exact source hashes. This batch adds **29 theorems in five modules**.
All **24 libraries build (10,252 Lake jobs)** and the exact **658-name axiom
audit passes**, with only `propext`, `Classical.choice` and `Quot.sound`.

**G.2 is proved**, including the multi-source union bound under the genuine
uniform Cartesian-grid law. Exposing one index and averaging its finite
fibres retains the correct source factor. All k*(Q+1) occurrences are included,
and actual stronger-good-set membership supplies the real kernel thresholds.
The ambient conditions Q<=n, Q<Y and 3*k*(Q+1)<Y remain explicit.

The Poisson-size ordered spatial cloud has an actual nonnegative, normalized
countable mass function. Event summability and averaging identities are proved.
For K=ceil(2*Lambda), its failure probability is bounded by
`Lambda*omitted/n + K*(Q+1)*T^(-1+log(3*K*(Q+1))/log(Y))
+ exp(-(2*log(2)-1)*Lambda)`.
The exact generating series proves the Poisson tail without an extra constant.
Its convergence retains Lambda->infinity from Appendix F/G.

**G.3 remains partial:** identification with the full signed/excess target,
high marks and literal saddle-scale absorption/convergence remain. Subsequent
Palm and Fourier/cumulant obligations remain in the coverage ledger. This batch
uses no analytic/arithmetic literature premise. Earlier baseline inputs,
including the separate F.2 analytic solution premise, are unchanged.
No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts retain their historical source hashes and scope.
