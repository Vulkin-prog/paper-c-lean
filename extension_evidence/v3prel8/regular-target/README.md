# Complete regular target and literal saddle absorption

The [receipt](validation.json) binds **701 proved theorems in 144 modules**
to exact source hashes. This batch adds **43 theorems in seven modules**.
All **24 libraries build (10,259 Lake jobs)** and the exact **701-name axiom
audit passes**, with only `propext`, `Classical.choice` and `Quot.sound`.

The actual Poisson sample, carrying independent uniform sites, fair signs and
unbounded geometric excesses, has the complete existing target law. Its finite
position prefixes and count/grid atoms recover exactly the earlier ordered CRT
cloud mixture. The intrinsic regular set includes the empty configuration,
bounds total multiplicity and excludes every high excess. Its complement under
the full target has the expected bad-site, CRT, Poisson-tail and high-mark costs.

At the literal hard saddle, `nu^2/(u*V)` tends to infinity. Exact rounded
`K=ceil(2*Lambda)` and `Y=floor(exp V)` have the required logarithmic gap under
the information margin and logarithmic support band. The full CRT cost is
absorbed into `exp(-c*theta*nu^2/(4*u))`, which tends to zero. The source-facing
full-target theorem discharges every geometric and CRT side condition from the
paper length/information regime and gives
`Lambda*omitted/(M-L)+exp(-c*theta*nu^2/(4*u))
+exp(-(2*log 2-1)*Lambda)+Lambda/2^(E_*+1)`.

**G.3 remains partial at its final asymptotic assembly:** excluded-site and
high-mark estimates must still be combined with this bound and Lambda->infinity
for the complete displayed bound and o(1). The presence identity was proved in
an earlier batch. G.4/G.5 and Fourier/cumulant obligations remain open. This
batch uses no analytic/arithmetic literature premise; earlier explicit inputs,
including F.2, are unchanged. No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts retain their historical source hashes and scope.
