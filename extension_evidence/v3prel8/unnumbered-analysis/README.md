# Prescribed floors, tilted voids and weighted prime witnesses

The [receipt](validation.json) binds **1,087 theorems in 224 modules** to exact
source hashes. This checkpoint adds **39 theorems in 9 modules**. All
**24 libraries build (10,339 Lake jobs)**. The exact **1,087-name axiom audit**
passes, using only `propext`, `Classical.choice` and `Quot.sound`.

The full field at a separately prescribed information floor is proved under
the existing AGG/PNT inputs. Choosing max(w0,w_N(I)) respects the original
sigma-field, and the logarithmic support band follows from the source regime.
The finite tilted law, normalized void derivative and exact integral are
proved, including the +infinity improper limit when the void probability is
zero. The same arithmetic prime witness works for any fixed t, with lower
leading exponent log(2)*(log(1+t)-1) and divergence above t=exp(1)-1.

Named outside-deletion convergence, raw/centered cumulant conventions, the
pair-rank example and final unnumbered review remain. **Full realignment is
not yet complete.** F.2 retains its explicit analytic solution premise.
No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payloads** are unchanged.
Ten regression tests, source/coverage inventories and repository guards pass.
Historical receipts and complete compressed transcripts are preserved.
