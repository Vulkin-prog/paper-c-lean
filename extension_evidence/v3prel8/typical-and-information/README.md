# Typical dictionaries and information-adapted labelled fields

The [receipt](validation.json) binds **436 proved theorems in 92 modules** to
exact source hashes. This batch adds **90 theorems in 22 modules**. All
**24 libraries build (10,207 Lake jobs)** and the exact **436-name axiom audit
passes**, using only `propext`, `Classical.choice` and `Quot.sound`.

**Theorem 5.3 and proposition 6.2 are proved under the explicit baseline
process AGG and PNT inputs.** The typical-dictionary proof averages each
fixed-dictionary conditional error, including exact per-fibre deletion and
actual collision rank. Its finite bound retains `#mask*B`. A single supremum
rate and a constructed logarithmic envelope yield the literal uniform
little-oh remainder. The exceptional proportion, deterministic restrictions
and actual infinite iid comparison have the asserted rates.

The actual information saddle is constructed, with uniqueness, endpoints,
constrained optimization, strict improvement and the displayed leading limit.
The full signed conditional spatial law satisfies
`67*exp(-cprime*nu)+64*N^(-1/3+epsilon)` at the literal source regime, without
an additional length-band hypothesis. Both laws retain every excess after
removing an auxiliary larger deterministic truncation. All deterministic
readouts contract. The root/optimization/asymptotic proofs need no AGG/PNT input.

Affine sampling, Palm/cumulant complements and remaining unnumbered assertions
prevent a claim of complete alignment. The prescribed-floor leading optimum
is proved; its separate full field specialization remains to be assembled.
Unneeded derivative formulas are not newly formalized. F.2 retains its explicit
baseline analytic solution input. No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts remain historical and bound to their original source snapshots.
