# Independent tail and actual deletion bounds

The [receipt](validation.json) binds **174 proved theorems in 32 modules** to
exact source hashes. This batch adds 21 theorems. All **24 libraries build
(10,147 Lake jobs)** and the exact **174-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

The source tail is now controlled independently of the new microscopic field
comparison. Besides the scalar-law argument, a stronger masked first-moment
bound gives the actual excess-tail probability under any positive event.
The literal complement of G0 is translated from boundaries to starts and its
conditional hit probability is bounded by the square-root cutoff plus the
number of sites with a bad pivot, and the independent microscopic remainder.
The bad-pivot set here includes shallow sites; its count is a valid upper
bound rather than an identification with the paper's deep-only set.

These are intermediate probability bounds, not the full theorem 7.7. The
information-dependent cutoff must still be shown to satisfy the shifted band
and budget hypotheses; the bad-pivot count, relation-excess sum and final
uniform asymptotics remain to be completed. Other new families remain open.
The inherited arithmetic/literature inputs are explicit theorem arguments.

All 1,171 baseline Lean files and all 28 delivered manuscript files remain
unchanged. Ten source/audit regression tests, inventories and repository
root guards pass. Complete compressed build/audit logs are archived with
hashes of both the compressed and original bytes.

The [previous receipt](../microscopic-field/README.md) remains historical and
source-bound. This receipt is not a Palomar or NanoDa/Comparator qualification.
