# Dyadic restriction and common Markov-kernel readouts

The [receipt](validation.json) binds **265 proved theorems in 56 modules** to
exact source hashes. This batch adds **20 theorems**. All **24 libraries build
(10,171 Lake jobs)** and the exact **265-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

The common Markov-kernel clause of 7.8 is proved with sharp constant one, into
any measurable output space, including auxiliary marking followed by a
measurable readout. The argument centers [0,1]-valued tests at 1/2.

The dyadic endpoint 7.7a is proved under the explicit baseline analytic and
arithmetic inputs. Actual saddle monotonicity yields
`nu(H)<=nu(K)<=(K/H)*nu(H)`. This transfers the one-factor information budget
to 4N without implicit differentiation. Prime sigma-algebra inclusion and
exact integer-site restriction preserve the original conditioning event and
all positions, signs and unbounded excesses. The final bound is
`10*exp(-c_prime*nu_N)+4*N^(-1/3+epsilon)`, including the literal moving-depth
normalization. The stronger derivative estimate in the paper is not newly proved.

The analytic solution construction in F.2 remains an explicit premise.
Dictionary, information-adapted saddle, empirical and Palm/cumulant families
remain incomplete. This is not complete 3PREL8 alignment.
All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
The [previous receipt](../full-microscopic/README.md) remains historical and bound
to its original sources. No Palomar or NanoDa/Comparator qualification is claimed.
