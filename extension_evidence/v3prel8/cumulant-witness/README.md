# Signed Palm identities, finite cumulants and the arithmetic prime witness

The [receipt](validation.json) binds **927 theorems in 185 modules** to exact
source hashes. This checkpoint adds **133 theorems in 24 modules**. All
**24 libraries build (10,300 Lake jobs)**. The exact **927-name axiom audit**
passes, using only `propext`, `Classical.choice` and `Quot.sound`.

G.4 has the exact named retained regular target, its exceptional limit, actual
full/retained ordinary Palm deletion and literal normalized comparison with
an explicit 5*Lambda^2/n penalty. G.6 has actual arithmetic centered signed
coefficients, valuation matrices, a constructed full-rank plant and the
nonnegative normalized avoidance polynomial with its target-sign bound.

G.8's finite categorical TV bound, polynomial envelope and occupancy-log bound
are proved by finite moment/cumulant recursion. The proof only sums distinct-site
labelled blocks and bounds disjoint collections by a finite product. Higher
orders use centered moments; Palm singletons need their own reference convention.

G.7 has the exact two-rank identity, one-event absolute bound and one-target CRT
host count. G.9 has the actual prime witness, exact signed marks/count, retention,
original event probability and finite arithmetic activity lower bound. Their
remaining asymptotic assemblies, unnumbered review and prescribed-floor full-field
specialization prevent completion. F.2 retains its explicit analytic premise.
**Full realignment remains incomplete.** No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payloads** are unchanged.
Ten regression tests, source inventories and repository guards pass. Historical
receipts are preserved; the compressed transcripts include both hash forms.
