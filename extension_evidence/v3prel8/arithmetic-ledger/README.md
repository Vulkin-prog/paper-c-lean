# Bad-support arithmetic and relation-excess ledger

The [receipt](validation.json) binds **187 proved theorems in 34 modules** to
exact source hashes. This batch adds 13 theorems. All **24 libraries build
(10,149 Lake jobs)** and the exact **187-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

Actual bad supports are counted via injective translations into the odd-pivot
population. At the hard saddle this gives `n*exp(-V+epsilon*nu)`, uniformly
under the explicit population and logarithmic-support conditions. The actual
conditional deletion bound uses this estimate, and the information-budget
absorption is proved as a separate algebraic implication.

The infinite conditional field comparison now separates the linear local
pair count, the sparse directed baseline and the nonnegative full-value
relation excess over separated retained sites. Only the excess is extended
to all such pairs. The arithmetic profile of that remaining sum still needs
to be instantiated. Cutoff admissibility and the final uniform limits are
also open; this is not the complete theorem 7.7 or the full paper alignment.

All 1,171 baseline Lean files and all 28 delivered manuscript files remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts are compressed and hashed. The inherited
PNT, first-moment arithmetic and analytic Stein inputs remain explicit.

The [previous receipt](../discard-bounds/README.md) remains historical and
source-bound. No new Palomar or NanoDa/Comparator qualification is claimed.
