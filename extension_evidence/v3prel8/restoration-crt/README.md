# Full G.1 restoration and actual CRT allocation probabilities

The [receipt](validation.json) binds **629 proved theorems in 132 modules**
to exact source hashes. This batch adds **20 theorems in five modules**.
All **24 libraries build (10,247 Lake jobs)** and the exact **629-name axiom
audit passes**, with only `propext`, `Classical.choice` and `Quot.sound`.

**G.1 is proved with explicit baseline arithmetic inputs.** The full original
infinite conditioned spatial field is reduced to the actual independently
replaced low field, with completion error
`8*exp(-cprime*nu)+2*M^(-1/3+epsilon)+2*exp(-(c-theta)*nu/2)`.
The error tends to zero for theta<c, cprime>0 and epsilon<1/3.
No Stein solution or comparison for the replaced field is assumed by this
reduction. The baseline arithmetic premises remain explicit.

The all-prime CRT allocation bound is proved on the genuine uniform
Cartesian grid, using the actual odd rough kernel. Empty blocks cost one;
the factor three is charged per assigned prime. The threshold-power bound,
single-support regularity and deterministic hosting obstruction are proved.
These components use no analytic/arithmetic literature premise.

**G.2 remains partial:** the final multi-source probability union bound
remains, before the Poisson-size cloud estimate G.3. Subsequent Palm and
Fourier/cumulant obligations remain in the coverage ledger. The separate
F.2 analytic solution premise is unchanged. No new Palomar qualification
is claimed.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts retain their historical source hashes and scope.
