# Empirical full-vector support obstruction

The [receipt](validation.json) binds **346 proved theorems in 70 modules** to
exact source hashes. This batch adds **25 theorems**. All **24 libraries build
(10,185 Lake jobs)** and the exact **346-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

**Remark 7.8b is proved for every fixed realization without analytic or
arithmetic literature premises.** The empirical measure is the actual
uniform-origin pushforward, with every event mass proved equal to its
observed frequency. Its support has at most N configurations, including
repeated observations. On the event of at least two points, every atom of
the genuine product-Poisson target has mass at most p^2, giving the bound
`TV>=1-exp(-h*p)*(1+h*p)-N*p^2`.

The actual relative coordinates agree with the existing arithmetic starts
at integer positions u+i+2 for zero-based indices. At the literal paper
scales, `N*p^2->0` and `log(N*p^2)/log(M)->-1`. With `h*p->tau`, the result is
`liminf TV>=1-exp(-tau)*(1+tau)>0` for every fixed realization and the paper's
fixed 0<alpha<1, tau>0. No final convergence or numerical regime is assumed.

Corollary 7.8a from the [previous receipt](../empirical-paper/README.md)
retains the baseline directional Stein solution, ordinary PNT,
Laishram-Shorey and Nicolas-Robin inputs. The deterministic support
obstruction does not use those inputs. Typical/affine dictionaries,
information-adapted saddles and Palm/cumulant complements remain open.
F.2's analytic solution construction remains an explicit baseline premise.
Full 3PREL8 alignment is incomplete.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts remain historical and bound to their original sources.
No Palomar or NanoDa/Comparator qualification is claimed for these new
declarations.
