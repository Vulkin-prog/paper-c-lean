# Literal stronger-deletion endpoints

The [receipt](validation.json) binds **609 proved theorems in 127 modules**
to exact source hashes. This batch adds **35 theorems in seven modules**.
All **24 libraries build (10,242 Lake jobs)** and the exact **609-name axiom
audit passes**, with only `propext`, `Classical.choice` and `Quot.sound`.

The literal T_M threshold, its tilted power and floor are controlled exactly.
All support and reciprocal-tilt factors are absorbed, giving actual additional
counts n*exp(-V+(theta+epsilon)*nu) for every epsilon>0. Old bad-pivot exclusions
are already included in the stronger kernel condition and need not be charged
twice. Actual additional mass vanishes and the total complement is o(M/log M).

The independent replacement is a normalized actual Poisson mixture on the
original signed-coordinate space. Exact conditional arithmetic marginals give
cost <=2*p per deleted site, without an inverse-event-probability factor.
At the literal paper E_*, Y and information budget, all auxiliary geometry
is discharged: the replacement bound is 2*exp(-(c-theta)*nu/2), tending to zero.
The arithmetic counts retain the existing PNT remainder; no new literature
premise or final convergence/comparison hypothesis is added.

**G.1 remains partial at the final restoration assembly only:** the new
arithmetic, density and low-type replacement endpoints are proved, but their
combined source-facing composition with original whole-field deletions and
high-mark tails remains open. CRT/regular-cloud, subsequent Palm and
Fourier/cumulant obligations remain in the coverage ledger. No new Palomar
qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts retain their historical source hashes and scope.
