# Rough-kernel counts and stronger deletion components

The [receipt](validation.json) binds **574 proved theorems in 120 modules**
to exact source hashes. This batch adds **28 theorems in five modules**.
All **24 libraries build (10,235 Lake jobs)** and the exact **574-name axiom
audit passes**, with only `propext`, `Classical.choice` and `Quot.sound`.

The actual small-kernel count and nontrivial omega-weighted reciprocal sum
are proved through the canonical support injection. Both dispense with the
paper's zeta factor. The empty support is removed before the finite Euler
product; its tail uses integer integral comparisons. The weight parameter
is independent of the small-prime cutoff. The stronger good set has the
exact real-threshold/floor equivalence, the Q+1 multiplicity bound and an
exact original/additional deletion partition.

The original PNT remainder supplies the Euler prefactor uniformly for X>=M,
and the actual hard-cutoff count has prefactor exp(-V+epsilon*nu), with the
threshold factor explicit. Only the three saddle theorems use this literature
premise; the other 25 new theorems use none.

**G.1 remains partial.** Literal T_M threshold absorption, final little-oh
limits and the actual deletion coupling remain open. CRT allocation and
high-probability regular clouds are not asserted. The remaining Palm,
Fourier/Walsh and cumulant obligations are listed in the coverage ledger.
No new Palomar qualification is claimed.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
Earlier receipts remain historical and tied to their original source snapshots.
