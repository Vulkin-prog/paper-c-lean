# Arithmetic Palm completion and signed environment formulas

The [receipt](validation.json) binds **794 proved theorems in 161 modules**
to exact source hashes. This checkpoint adds **93 theorems in 17 modules**.
All **24 libraries build (10,276 Lake jobs)** and the exact **794-name axiom
audit passes**, with only `propext`, `Classical.choice` and `Quot.sound`.

G.3 is complete with baseline PNT, including its displayed five-term bound,
actual private-prime conditional presence and full target o(1) when Lambda
also tends to infinity. G.5 has the actual arithmetic normalized Palm deficit
bound on every retained mask, at the paper regime with the same explicit
inputs as 7.7. The extra normalization cost is at most M^(-1/2). Mean and
probability completion use the original changing countable target spaces.

The true infinite conditional source masses and full signed/excess Poisson
atoms satisfy the exact Palm mass identity. Ordinary deletion cancels the
exponential factor before summing. Almost-sure run termination, preserved
under both conditionings, identifies the void with absence of remaining
base starts. The finite G.4 normalized comparison uses actual source and
target deletion costs. Its precise G.3 named-set transfer through coordinates
and restriction remains to be recorded.

G.6 now includes actual Walsh inversion and energy, the sharp sign deficit
bound, exact affine fibre characters, and the arbitrary environment Fourier
transform. Planting a full-row-rank system preserves that environment law.
The centered-product coefficient assembly and full arithmetic matrix
instantiation remain. Pair activity, cumulants/obstruction, unnumbered review
and the separate prescribed-floor specialization also remain open. F.2's
baseline analytic solution premise is unchanged. Full realignment is incomplete.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Build/audit transcripts have compressed and uncompressed hashes. No new
Palomar qualification is claimed. Earlier receipts remain historical.
