# The actual prime-subsequence obstruction and finite Palm criterion

The [receipt](validation.json) binds **1,005 theorems in 202 modules** to exact
source hashes. This checkpoint adds **78 theorems in 17 modules**. All
**24 libraries build (10,317 Lake jobs)**. The exact **1,005-name axiom audit**
passes, using only `propext`, `Classical.choice` and `Quot.sound`.

G.9's positive normalized lower limit and total-activity divergence are proved
for the actual G0 and each fixed G_theta. The literal prime windows, paper
cutoff, original low marginals, omission count including terminal sites, and
minimal M+Q cylinder are all discharged. Only the existing PNT premise remains
in this obstruction. The independent full microscopic distance tends to zero
under the existing F.2 directional Stein and arithmetic inputs.

The exact pair/higher decomposition is recorded. G.9's remaining divergence
at orders >=3 still needs G.7's actual vanishing pair estimate. The finite G.6
Palm cumulant criterion includes its nonzero singleton corrections.

G.7, final unnumbered review and the prescribed-floor full-field specialization
remain open. **Full realignment remains incomplete.** No new Palomar
qualification is claimed; F.2 retains its explicit analytic solution premise.

All **1,171 baseline Lean files** and **28 manuscript payloads** are unchanged.
Ten regression tests, source/coverage inventories and repository guards pass.
Historical receipts are preserved. Full compressed build/audit transcripts
include both compressed and uncompressed hashes.
