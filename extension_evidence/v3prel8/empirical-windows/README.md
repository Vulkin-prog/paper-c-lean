# Empirical overlapping-window counts

The [receipt](validation.json) binds **296 proved theorems in 61 modules** to
exact source hashes. This batch adds **31 theorems**. All **24 libraries build
(10,176 Lake jobs)** and the exact **296-name axiom audit passes**, using only
`propext`, `Classical.choice` and `Quot.sound`.

The actual product Poisson field has independent disjoint-window indicators.
Each origin has at most 2h-1 overlapping origins. Their covariance is at most
1/4, giving the exact variance bound `(2h-1)/(4N)`. Contained windows have
Poisson(h*p) counts and the corresponding exact empirical frequency mean.
One whole-frequency event transfers with only one full-field TV error:
`delta+(2h-1)/(4*N*eta^2)`.

The genuine arithmetic starts are ordered at integer positions i+2; summing
all signs and excesses agrees almost surely by run finiteness. Their actual
empirical tail bound is proved. Normalized frequency masses converge almost
surely in TV under explicit summable field errors and h/N ratios and limiting
window means, without independence or target couplings across scales.
The full-field error is summable on literal dyadic heights; window rounding
changes its mean by less than one site rate, and Poisson masses are continuous.

**7.8a remains partial.** Its literal length/window sequence still needs the
eventual microscopic regime, information budget, h/N summability and vanishing
site rate verified. The support obstruction 7.8b also remains open.
The analytic solution construction in F.2 remains an explicit premise.
Dictionary, information-adapted saddle and Palm/cumulant families remain
incomplete. This is not complete 3PREL8 alignment.

All **1,171 baseline Lean files** and **28 manuscript payload files** remain
unchanged. Ten regression tests, inventories and repository guards pass.
Complete build/audit transcripts have compressed and uncompressed hashes.
The [previous receipt](../dyadic-readouts/README.md) remains historical and bound
to its original sources. No Palomar or NanoDa/Comparator qualification is claimed.
