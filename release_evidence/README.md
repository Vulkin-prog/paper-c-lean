# v0.48.1 release evidence

This directory is intentionally empty at final source commit Q.  After a fresh
hardened local run bound to Q, its unique child packaging commit R adds only
the two privacy-minimized `result-*.json` files and
`v0.48.1/release-binding.json` below `v0.48.1/`.  Every record carries Q; R is
identified independently by Git and is never inserted into the binding.

Do not copy raw host transcripts here.  The public archive itself is published
as a GitHub release/Zenodo asset and identified by SHA-256.
