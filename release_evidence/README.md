# v0.48.1 release evidence

This directory is intentionally empty until a fresh hardened local run is
completed on the final release commit. Do not copy raw host transcripts here.
The qualified commit should contain only the two privacy-minimized
`result-*.json` files and `release-binding.json`; the public archive itself is
published as a GitHub release/Zenodo asset and identified by SHA-256.
